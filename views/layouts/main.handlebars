<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>Example App</title>
    </head>
    <body>
        {{{body}}}
        {{#each posts}}
            <a href="/{{url}}">
                <h1>{{title}}</h1>
            </a>
            <p>{{body}}</p>
            <p>Likes: {{likes}}</p>
            <p>post time: {{creationTime}}</p>
        {{/each}}
    </body>
</html>